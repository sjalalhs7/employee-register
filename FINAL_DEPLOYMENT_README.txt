MSM LONGROLL — FINAL UI / REGION / SECTOR UPDATE

1) GitHub Pages
- Replace repository index.html with this package's index.html.
- Upload msm-security-guards-logo.png beside index.html.
- Upload manifest.webmanifest, sw.js, icon-192.png, icon-512.png and icon-512-maskable.png beside index.html.
- Hard refresh Chrome (or clear site data) after deployment.

2) Firestore
- Publish firestore.rules in Firebase Console > Firestore Database > Rules.

3) What this update changes
- Upper professional navigation on parent and sector home pages.
- Red/blue MSM branded colour treatment.
- Company logo watermark on signed-in home/sector portal.
- 3D diamond-style animated record counter.
- Region cards reveal their sectors; old standalone sector strip is hidden.
- Parent Region/Sector Manager has robust Add Region, Add Sector and Assign controls.
- Sectors can be assigned/moved between any Regions without deleting employee records.
- Quick Access and Recycle Bin remain available.
- Parent account email/role is visible in the upper navigation after login.
- Public login page has Home / About / Services / Contact / Login / Install App navigation.
- Install App button uses the existing PWA install flow.

4) APK reality
- The website can be installed as an Android app (PWA) directly from Chrome.
- A true signed .apk is not generated by the HTML file itself. Use PWABuilder/Bubblewrap after the live PWA is deployed if a standalone APK/AAB is required.

5) Test sequence
Parent login -> Regions/Sectors -> Add Region -> Add Sector -> assign Sector to Region -> click Region -> open Sector -> verify records -> Recycle Bin -> Import Long Roll -> Sign out -> Sector Gmail login -> verify same sector records.
